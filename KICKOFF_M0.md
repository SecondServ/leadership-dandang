# 开工指令 · M0（粘贴给 Claude Code）

> 用法：把本仓库里的 `PRD.md` 和 `BUILD_SPEC.md` 放在项目根目录，然后把下面「=== 指令开始 ===」到「=== 指令结束 ===」之间的内容整段粘贴给 Claude Code。

---

=== 指令开始 ===

你要帮我从零搭一个 macOS 原生菜单栏 App，代号 Sidekick。完整规格见仓库根目录的 `PRD.md`（产品需求）和 `BUILD_SPEC.md`（技术方案），请**先把这两份读完**再动手。

**本次范围：只做 BUILD_SPEC 第 9 节的 M0（最小可行链路），别做 M1 及以后的任何功能。** 只实现 7 个功能里的「润色」这一个，用来把"选中文本 → 调 Gemini → 出结果"这条链路打通。

## M0 要交付的东西

1. **Xcode 工程骨架**：macOS App，Swift + SwiftUI/AppKit 混合，`LSUIElement = true`（菜单栏常驻、无 Dock 图标）。目录结构按 BUILD_SPEC 第 3 节来（这次只需建 M0 用到的文件，其余可留空占位）。
2. **菜单栏入口**：状态栏图标 + 菜单（含「设置」「退出」）。
3. **设置页**：一个输入框填 Gemini API Key，保存到 **Keychain**（不要存明文/UserDefaults）。再加一个只读说明："请使用 Gemini 付费 API Key，勿用免费 AI Studio Key。"
4. **全局快捷键**：注册一个可用的全局热键（如 ⌥⌘P），按下即触发下面的流程。（M0 先用快捷键触发，悬浮工具条是 M1 的事，这次不做。）
5. **文本抓取 `TextGrabber`**：M0 先只实现 **Cmd+C 回退方案**（BUILD_SPEC 第 5 节）——备份剪贴板 → 模拟 Cmd+C → 读剪贴板 → 还原原剪贴板。AX 直读选区留到 M1。
6. **`GeminiClient`**：按 BUILD_SPEC 第 6 节调 `generateContent`，M0 用快档模型（如 `gemini-2.5-flash`，做成常量方便改），temperature 0.5。
7. **「润色」Prompt**：用 BUILD_SPEC 第 7.3 节的模板原文，别自己改写。
8. **结果卡片 `ResultCardPanel`**：展示润色结果，提供「复制」和「关闭」；生成中显示 loading；失败显示错误 + 「重试」。M0 可以先用普通窗口，非激活 NSPanel 行为留到 M1。
9. **权限**：首次需要辅助功能/输入监控时，弹一个说明并提供跳转「系统设置」的按钮。
10. **ad-hoc 签名**：加一个 `scripts/sign-macos.sh`（`codesign -s -`）并固定 bundle id，让辅助功能授权在重编译后不掉。
11. **README**：如何构建运行、如何授权、如何填 Key；带上隐私说明（只在用户触发时发送选中文本、Key 只存本地、公司内容需先确认公司政策）。

## 硬约束（不许违反）

- **人在环中**：绝不自动向任何 App 发送消息，只做剪贴板/复制。
- **不做后台采集**：只在用户按快捷键那一刻读取当前选中文本。
- **日志安全**：任何日志都不许打印 API Key 或用户文本。
- **不越界**：本次只做 M0 + 「润色」一个功能，不要顺手实现其它 6 个功能或悬浮工具条。
- 把 `AI/`（GeminiClient / Prompts / Feature 定义）写得内聚、少依赖 AppKit，方便将来复用逻辑。

## 完成标准（M0 验收）

在 Slack 桌面客户端里选中一段文字，按全局快捷键，能弹出该文字被润色后的结果，并能一键复制；未填 Key、网络失败、429 都有友好提示。

## 交付方式

请：
1. 先给我一个**简短实现计划**（要建哪些文件、关键点、你打算怎么处理全局快捷键和 Cmd+C 回退的时序），等我确认后再写代码。
2. 写完后给出**手动验证步骤**（怎么授权、怎么填 Key、怎么测通）。
3. 如果遇到 NSPanel/AX/权限相关的坑，停下来告诉我,别反复试。

=== 指令结束 ===

---

## 后续里程碑（M0 通过后再逐条发给 Claude Code）

- **M1**：`SelectionMonitor` + `ActionBarPanel`（AX 读选区 + 鼠标监听 + **NSPanel 非激活浮窗**）；接功能 1、2、6（先做 Profile 页）。
- **M2**：`ReplyComposer`（功能 4：输入框 + 5 立场预设）；功能 5、7（研究模式开关）；`TextReplacer` 接 3/4/7。
- **M3**：Profile 完整化、功能开关/排序、语言策略、双档模型、权限引导页、错误/限流、README 完善。

> 提醒：M1 的 **NSPanel 非激活浮窗**是整个项目最容易翻车的点，建议 M1 一上来就先单独验证"划词弹条且不丢原 App 选区"，再接功能。
