# PRD —— 职场沟通辅助工具（工作代号：Sidekick）

> 版本 v0.3 · 目标平台：macOS 先行（原生 Swift），Windows 之后单独原生（C#/.NET）· 交付对象：Claude Code / 开发者
> 命名可随时替换；文中 "Sidekick" 均为占位代号。

---

## 1. 产品定位

Sidekick 是一个**系统级 macOS 工具**，帮助用户在日常职场沟通中降低心理与生理损耗。
用户在任意通信 App（Slack、飞书、企业微信、Teams、邮件、浏览器网页等）里**选中一段文字**后，会自动弹出一个**悬浮工具条（PopClip 式）**，提供一组围绕"读懂对话 / 写好回复 / 保护自己"的 AI 能力。

一句话价值主张：**把"看懂潜台词、组织得体回复、判断这事关不关我"这类隐形情绪劳动外包给 AI，让打工更省力。**

### 设计原则
1. **零摩擦触发**：划词即出顶层工具条，绝不进二级菜单。
2. **人在环中**：AI 只产出"草稿"，一律经用户确认后再由用户自己发送；工具本身不代发消息。
3. **本地优先、可控**：仅在用户点击某个动作时才把选中文本发往模型；API Key 存本地 Keychain。
4. **轻量常驻**：菜单栏 App，无 Dock 图标，不打扰。

---

## 2. 目标用户与场景

- 主要用户：需要频繁用 IM/邮件与同事沟通的知识工作者、职场新人、非母语工作者、社恐/高敏感人群。
- 典型场景：
  - 看不懂同事绕来绕去的长消息 → "让 ta 说人话"。
  - 群里吵成一团不知道各方立场 → "ta 们在讨论啥？"。
  - 想回复但措辞纠结、怕得罪人 → "帮我回复" / "润色"。
  - 被 @ 了但不确定关不关自己 → "关我毛事？"。
  - 想在讨论中体现价值感 → "刷存在感"。
  - 情绪上头想还嘴又要体面 → "帮我阴阳 ta"。

---

## 3. 功能总览（7 个动作）

| # | 名称 | 类型 | 输入 | 输出 | 需要 Profile |
|---|------|------|------|------|--------------|
| 1 | 让 ta 说人话 | 只读 | 选中文本 | 目标语言的简洁口语化翻译 | 否 |
| 2 | ta 们在讨论啥？ | 只读 | 选中文本 | 目标语言的分人总结 | 否 |
| 3 | 润色 | 改写 | 选中文本 | 更专业清晰的同语言版本 | 可选 |
| 4 | 帮我回复 | 交互 | 选中文本 + 用户主旨 + 立场预设 | 正式专业的回复草稿 | 可选 |
| 5 | 帮我阴阳 ta | 改写 | 选中文本 | 礼貌但略带讽刺的回应草稿 | 可选 |
| 6 | 关我毛事？ | 只读/分析 | 选中文本 + Profile | 相关性判断 + 任务/影响提取 | **是** |
| 7 | 刷存在感 | 生成/研究 | 选中文本 + Profile | 有价值的专业见解/方案草稿 | 可选 |

> 目标语言（Target Language）当前仅"中文"，设计上预留为可配置项。

---

## 4. 功能详细规格

对每个功能定义：目的、输入、AI 行为、输出形态、边界。
（各功能对应的具体 Prompt 模板见《BUILD_SPEC.md》第 7 节。）

### 4.1 让 ta 说人话
- **目的**：把晦涩/绕/夹杂行话的对话，转成目标语言的大白话。
- **输入**：选中文本。
- **AI 行为**：翻译成目标语言（默认中文）；尽量简洁但**不丢关键信息**；口语化、大白话，去掉客套与废话。
- **输出**：只读结果卡片（可复制）。
- **边界**：不评价、不加建议，只做"翻译成人话"。

### 4.2 ta 们在讨论啥？
- **目的**：快速掌握多人对话里各方立场。
- **输入**：选中文本（通常是一段多人对话）。
- **AI 行为**：先识别发言人，再**按人**总结每个人表达了什么（观点/诉求/情绪），翻译成目标语言。若能识别出结论或待决项，附一行"当前结论 / 悬而未决"。
- **输出**：只读结果卡片，结构为「谁 → 说了啥」列表 + 可选一行结论。
- **边界**：忠实总结，不替对话下判断。

### 4.3 润色
- **目的**：把用户自己写的草稿打磨得更专业、清晰、得体。
- **输入**：选中文本（用户自己的草稿）。
- **AI 行为**：保持**原文语言**（英文进英文出、中文进中文出）；改善清晰度、专业度、语气自然度；不改变原意、不夸大。
- **输出**：结果卡片，提供「复制」与「替换原文」（把结果写入剪贴板并模拟 Cmd+V 覆盖选区）。
- **边界**：默认商务专业语气；预留语气档位（正式/中性/亲切）为后续迭代。

### 4.4 帮我回复
- **目的**：根据对方消息 + 用户简短主旨，生成正式专业的回复。
- **输入**：
  - 选中文本（对方/对话内容，作为 context）。
  - 用户在输入框输入的**回复主旨**（简明一句话）。
  - **立场预设**（单选）：同意 / 婉拒 / 应付 / 共情 / 甩锅。
- **AI 行为**：结合对话、主旨、所选立场，写出 professional、formal 的回复；语言默认跟随对话语言（可在设置里强制目标语言）。
- **输出**：可编辑的结果卡片，提供「复制」「替换/粘贴」「重写（regenerate）」。
- **交互**：点击该动作后展开带输入框 + 5 个立场按钮的面板；主旨可留空（留空则由 AI 依立场自拟）。
- **边界**：只生成草稿，不发送。立场"甩锅"应表现为**得体地澄清边界/转移责任归属**，而非攻击他人。

### 4.5 帮我阴阳 ta
- **目的**：在需要"体面地表达不满"时，给出礼貌但略带讽刺/挖苦的回应。
- **输入**：选中文本。
- **AI 行为**：生成表面礼貌、职场可发送、但暗含讽刺/绵里藏针的回应；保持"可辩解的得体"（plausibly deniable），不脏话、不人身攻击。
- **输出**：结果卡片（复制/重写）；卡片上带一行温和提醒："发送前请自行判断风险"。
- **边界**：为降低风险，默认输出**较克制**版本；提供"再阴一点/收敛一点"两个微调按钮为后续迭代。这是最敏感的功能，必须强调人在环中。

### 4.6 关我毛事？
- **目的**：判断这段内容**是否与用户相关**，以及有没有落到用户头上的任务/计划/事件。
- **输入**：选中文本 + 用户 Profile（角色、职责、所在团队、关注事项）。
- **AI 行为**：总结内容；提取其中的任务 / 计划 / 事件；结合 Profile 判断对用户的**直接或间接影响**，给出相关性分级（高/中/低/无关）与理由；若有需用户行动的项，列出「建议动作」。
- **输出**：只读结果卡片，结构：相关性结论 → 关键事项 → 对你的影响 → 建议动作（如有）。
- **边界**：判断基于所给文本与 Profile，需提示"仅供参考"。

### 4.7 刷存在感
- **目的**：帮用户在讨论中**有质量地发声**，体现专业价值。
- **输入**：选中文本 + Profile（用于对齐口吻与专业领域）。
- **AI 行为**：基于对话内容，产出有价值的专业**见解/方案/想法**；允许更深的推理与结构化思考（deep-think）；必要时可开启"研究模式"（联网检索补充事实）。
- **输出**：可编辑结果卡片，通常为 1 段观点 + 2~3 条要点/建议；提供「复制」「重写」「更简短/更详细」。
- **边界**：内容需可信、贴合上下文，不空喊口号；研究模式为可选（默认关闭以控成本与时延）。

---

## 5. 通用交互流程

1. 用户在任意 App 选中文本。
2. Sidekick 检测到选区 → 光标附近弹出**悬浮工具条**（7 个图标/短标签）。
3. 用户点击某功能：
   - 只读/改写类（1/2/3/5/6）：直接抓取选中文本 → 调模型 → 弹出**结果卡片**。
   - 交互类（4）：先展开输入框 + 立场预设 → 用户填写 → 调模型 → 结果卡片。
   - 生成/研究类（7）：直接生成，或在结果卡片里可切换"研究模式"重跑。
4. 结果卡片统一提供：复制、（可用时）替换原文/粘贴、重写、关闭。
5. 全程不自动发送；由用户复制/粘贴回原 App 后自行发送。

### 触发的兜底
- 悬浮工具条基于全局选区检测；对检测不稳的 App（如 Electron），提供**全局快捷键**作为备用触发方式。
- 文本抓取：优先 Accessibility 读取选区，失败则回退到"模拟 Cmd+C 读剪贴板并还原"（详见 BUILD_SPEC）。

---

## 6. 用户 Profile 与设置

**Profile（供功能 6/7 及全局个性化使用）：**
- 姓名 / 昵称
- 公司角色、职级（如：产品经理 / P6）
- 所在团队、主要职责
- 关注事项 / 负责的项目（自由文本）
- 语言偏好（母语、常用工作语言）

**设置项：**
- Gemini API Key（存 Keychain）。
- 目标语言（当前：中文）。
- 回复类功能输出语言策略：跟随对话语言 / 强制目标语言。
- 各功能开关与在工具条中的排序。
- 触发方式：悬浮工具条开关、全局快捷键设置。
- 模型档位（快/强，见 BUILD_SPEC）、研究模式默认开关。

---

## 7. 非目标（本期不做）

- 不做自动发送 / 代替用户操作 IM。
- 不做消息的持续监听、抓取、存档（只处理用户主动选中的文本）。
- 本期先交付 macOS（原生 Swift）；**Windows 为之后单独的原生实现**（C#/.NET），两端只共享本 PRD 与 Prompt 规格，不共享代码。移动端不做。
- 不做团队/多用户、账号体系、云端同步。
- 不做对话历史的长期存储与训练。

---

## 8. 隐私与合规（必须在 README 中显著说明）

- 仅在用户点击动作时发送选中文本；不做后台采集。
- 模型侧：使用 **Gemini 付费 API**（Google 声明付费档不用于训练）；**严禁使用免费 AI Studio 档**（免费档会用于产品改进/人工标注）。
- 提示用户：若在**公司账号/内部内容**下使用，需自行确认公司政策是否允许内部通讯发往外部 AI 服务——"不被训练"不等于"合规允许外传"。
- API Key 仅存本地 Keychain，不上传。

---

## 9. 成功标准（个人自用视角）

- 划词到出工具条延迟 < 300ms；主流 App（Slack 桌面、飞书、Chrome）文本抓取成功率高。
- 7 个功能均可端到端跑通，结果可一键复制/粘贴回原 App。
- 敏感功能（4-甩锅 / 5-阴阳）默认克制、稳定不越界。
